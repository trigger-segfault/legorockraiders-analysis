
/*#include "External.h"

using namespace lego::globals;

#ifndef DEFINE_EXTERNALS
#define defExtern(type, address) = *(type)address
#define EXTERN 
#else
#define defExtern(type, address)
#define EXTERN extern
#endif

namespace lego {
namespace globals {

volatile char* (&g_AbilityTypes_TABLE)[6] = *(volatile char* (*)[6])0x004ebd60;

volatile unsigned int(&g_BuildingsTeleported) = *(volatile unsigned int (*))0x004ebdc4;




}
}*/

